#include "Game.hpp"

// Program entry point: create the game (which wires up the model, view and
// controller) and run its main loop until the window closes.
int main() {
    Game game;
    game.run();
    return 0;
}